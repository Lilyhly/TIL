init에서 print(label.text)를 찍으면 옵셔널이 찍히는 이유는 UILabel의 text 속성이 Optional String으로 선언되어 있기 때문입니다. 이는 해당 속성이 값을 가지지 않을 수도 있기 때문에 Optional로 정의되어 있습니다. 이 경우 옵셔널 체이닝이나 옵셔널 바인딩을 사용하여 안전하게 처리할 수 있습니다.
참조(reference)란 객체나 인스턴스에 대한 식별자(identifier)로, 해당 식별자는 해당 객체 또는 인스턴스를 식별하고 참조합니다. 다시 말해, 참조는 메모리 내의 특정 위치를 가리키는 것입니다.
할당(assignment)은 변수나 상수에 값을 할당하는 작업을 의미합니다. 이는 변수 또는 상수에 값을 저장하는 것을 말합니다.
getter와 setter는 프로퍼티에 접근하는 방법을 제어하는 메서드입니다. getter는 프로퍼티의 값을 반환하고, setter는 프로퍼티에 값을 설정합니다.
return은 함수나 메서드에서 값을 반환하는 키워드입니다. 함수 또는 메서드 내에서 처리한 결과를 호출한 쪽으로 반환할 때 사용됩니다.
의존성(dependency)은 한 요소가 다른 요소에 의존하여 동작하는 것을 의미합니다. 예를 들어, 클래스 A가 클래스 B를 사용하여 동작한다면, 클래스 A는 클래스 B에 의존성을 가지고 있습니다.
강한 결합(strong coupling)은 한 요소가 다른 요소와 너무 밀접하게 결합되어 있는 것을 의미합니다. 이는 코드를 변경할 때 한 요소의 수정이 다른 요소에 영향을 미칠 가능성을 높입니다.
ARC(Automatic Reference Counting)는 메모리 관리 기법 중 하나로, 애플리케이션의 객체에 대한 참조 횟수를 추적하여 더 이상 사용되지 않는 객체를 자동으로 해제하는 방식입니다.
"has no member" 오류는 특정 멤버에 접근할 수 없는 경우 발생하는 오류입니다. 이는 해당 멤버가 정의되지 않았거나 오타가 있는 경우에 발생할 수 있습니다.

init方法中使用print(label.text)打印时出现可选值的原因是因为UILabel的text属性被声明为可选字符串(Optional String)。这是因为该属性可能不会始终具有值，因此被定义为可选类型。您可以使用可选链(Optional Chaining)或可选绑定(Optional Binding)来安全地处理这种情况。
引用(reference)是指对对象或实例的标识符，该标识符引用并标识该对象或实例。换句话说，引用是指向内存中特定位置的指针。
赋值(assignment)是将值分配给变量或常量的操作。这意味着将值存储到变量或常量中。
getter和setter是控制对属性访问的方法。getter用于返回属性的值，setter用于设置属性的值。
return是从函数或方法中返回值的关键字。它用于在函数或方法中处理的结果返回给调用方。
依赖(dependency)是指一个元素依赖于另一个元素以执行操作。例如，如果类A使用类B来执行操作，则类A具有对类B的依赖性。
强耦合(strong coupling)是指两个元素之间关联过于紧密的情况。这会导致在更改代码时，一个元素的修改可能会影响到另一个元素。
ARC(Automatic Reference Counting)是一种内存管理技术，它通过跟踪对对象的引用计数来自动释放不再使用的对象。
"has no member"错误表示无法访问特定成员。这可能是因为该成员未定义或存在拼写错误。
